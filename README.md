Mahasiswa-Microservices-Server

List of end point :

1. findAll 
	url    : "/mahasiswa"
	method : GET
	return : semua data mahasiswa yang ada di database
	
2.  fingById
	url		: "/mahasiswa/{id}"
	method	: GET
	return	: data mahasiswa dengan Id sesuai dengan parameter
